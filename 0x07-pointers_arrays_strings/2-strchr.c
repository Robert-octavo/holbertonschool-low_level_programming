#include "main.h"

/**
* strchr - function that locates a character
* in a string
* @s: pointer to a char
* @c: char
*
* Return: pointer
*/
char *_strchr(char *s, char c)
{
	int i;

	i = 0;

	while (*(s + i) != c)
		i++;

	while (*(src + i))
	{
		return ((s + i));
		i++;
	}

}
