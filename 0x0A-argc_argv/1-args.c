#include <stdio.h>
/**
* main - prints the name of the numbers of
* arguments passed into it
* @argc: argument count
* @argv: array of arguments
* Return: Always 0
*/
int main(int argc, char *argv[])
{
	printf("%s\n", argc - 1);

	return (0);
}
