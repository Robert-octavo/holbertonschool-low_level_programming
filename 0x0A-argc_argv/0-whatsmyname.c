#include <stdio.h>
/**
* main - printrs the name of the program
* @agrc: argument count
* @argv: array of pointers
*
* Return: Always 0
*/
int main(int argc, char *argv[])
{
	agrc = agrc;
	printf("%s\n", argv[0]);

	return (0);
}
